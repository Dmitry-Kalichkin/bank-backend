server:
  port: 8081
  servlet:
    context-path: /api/v2/accounts

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0